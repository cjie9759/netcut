package controller

import (
	"github.com/gin-gonic/gin"
)

type CutCtr struct{}

func (T *CutCtr) Get(c *gin.Context) {
	// var p param.Pagsse.NewTor().WebList(&p, &data)
	// d := make(map[string]interface{}, 5)
	// d["data"] = data
	// d["count"] = count
	// c.JSON(http.StatusOK, d)
}
